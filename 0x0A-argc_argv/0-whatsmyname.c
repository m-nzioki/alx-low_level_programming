#include "main.h"

/**
 * main - Prints its name
 * @argc: arguments count
 * @argv: array of pointers to the string arguments passed
 *
 * Return: 0 when successful
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}

